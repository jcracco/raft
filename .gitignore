# RAFT — gitignored files

# Config and bootstrap (contain credentials and server paths)
config.php
bootstrap.php

# Local only
other/
config.local.php
.local

# Session tokens (runtime file, contains active login tokens)
session_tokens.json

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
*.swp
